import styled from 'styled-components'

export const StyledNews = styled.div`
    overflow: hidden;
    min-width: 192rem;
    background-color: #F8F8FA;

    h2 {
        font-weight: 700;
        font-size: 4rem;
        text-transform: uppercase;
        color: #000;
        margin: 9.35rem 119.7rem 8.7rem 25rem;
    }
`